<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Permite que o motor JavaScript V8 use compilação Just-In-Time (JIT) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Permite que o app aloque memória executável não assinada (usado pelo V8) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Se seu app usa módulos nativos que não são assinados com seu Developer ID,
         ou se você tem problemas com o Hardened Runtime bloqueando-os,
         esta chave pode ajudar. Use com cautela. -->
    <!-- <key>com.apple.security.cs.disable-library-validation</key> -->
    <!-- <true/> -->

    <!-- Se seu app precisa fazer conexões de rede de saída -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Adicione outros entitlements conforme necessário para as funcionalidades do seu app -->
    <!-- Exemplo: Acesso a arquivos do usuário com permissão
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    -->
</dict>
</plist>